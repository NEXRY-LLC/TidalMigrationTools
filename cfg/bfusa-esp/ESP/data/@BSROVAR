/**********************************************************************/
/*THESE ARE THE GET VARIABLE STATEMENTS NEEDED FOR ANY BSRO APPL      */
/**********************************************************************/
VGET (APPSIFMX) TABLE(bsro) CLANG             /* Get the variable */
VGET (APPTMP) TABLE(bsro) CLANG             /* Get the variable */
VGET (BENOW) TABLE(bsro) CLANG             /* Get the variable */
VGET (BFMIXBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (BIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (BLUEMARTINI) TABLE(bsro) CLANG             /* Get the variable */
VGET (BNSBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (BONUSDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (COMPANY1) TABLE(bsro) CLANG             /* Get the variable */
VGET (COMPANY9) TABLE(bsro) CLANG             /* Get the variable */
VGET (DGPBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (HEMBNDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (HEMHOME) TABLE(bsro) CLANG             /* Get the variable */
VGET (INBOUND) TABLE(bsro) CLANG             /* Get the variable */
VGET (INVBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (JBLDDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JD570) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAADMIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDABIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDALOG) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAOPERA) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDASFTP1) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAUSBTC) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAUSDTL) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAUSORW) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAUSSTP) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDAUSTBC) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDBASDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDBINDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JDORAREP) TABLE(bsro) CLANG             /* Get the variable */
VGET (JNASRVDR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JNAXFDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JNAXFNEW) TABLE(bsro) CLANG             /* Get the variable */
VGET (JPOSFDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JRECLDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JUPLTODO) TABLE(bsro) CLANG             /* Get the variable */
VGET (JUSLAXFR) TABLE(bsro) CLANG             /* Get the variable */
VGET (JUSNAXFR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LAWADM) TABLE(bsro) CLANG             /* Get the variable */
VGET (LAWBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (LAWDATA) TABLE(bsro) CLANG             /* Get the variable */
VGET (LAWPRM) TABLE(bsro) CLANG             /* Get the variable */
VGET (LAWSH) TABLE(bsro) CLANG             /* Get the variable */
VGET (LBLJOBS) TABLE(bsro) CLANG             /* Get the variable */
VGET (LOBJDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LOYALTY) TABLE(bsro) CLANG             /* Get the variable */
VGET (LPLINE) TABLE(bsro) CLANG             /* Get the variable */
VGET (LPRTDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSACJOB) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSADMIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSAPJOB) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSAPUSR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSARDLC) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSARJBS) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSARNAC) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSARRNT) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSBLJBS) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSBLJOB) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSBLNAC) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSBLRNT) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSCBJBS) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSGLJBS) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSGLSTR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSGLUSR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSMAJBS) TABLE(bsro) CLANG             /* Get the variable */
VGET (LUSNAXFR) TABLE(bsro) CLANG             /* Get the variable */
VGET (LWRKDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (MAESTRO) TABLE(bsro) CLANG             /* Get the variable */
VGET (N) TABLE(bsro) CLANG             /* Get the variable */
VGET (NARUNBAT) TABLE(bsro) CLANG             /* Get the variable */
VGET (NAXFERDR) TABLE(bsro) CLANG             /* Get the variable */
VGET (OBJ) TABLE(bsro) CLANG             /* Get the variable */
VGET (OMNIBACK) TABLE(bsro) CLANG             /* Get the variable */
VGET (OMNIORA8) TABLE(bsro) CLANG             /* Get the variable */
VGET (OMNIORCL) TABLE(bsro) CLANG             /* Get the variable */
VGET (OPSID) TABLE(bsro) CLANG             /* Get the variable */
VGET (OUTBOUND) TABLE(bsro) CLANG             /* Get the variable */
VGET (PL) TABLE(bsro) CLANG             /* Get the variable */
VGET (POSADMIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (POSBNDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (POSECSDS) TABLE(bsro) CLANG             /* Get the variable */
VGET (POSFIDIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (POSFODIR) TABLE(bsro) CLANG             /* Get the variable */
VGET (PRDBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (PRDGREP) TABLE(bsro) CLANG             /* Get the variable */
VGET (PRDSH) TABLE(bsro) CLANG             /* Get the variable */
VGET (PRINT) TABLE(bsro) CLANG             /* Get the variable */
VGET (PRODBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (PRODSQL) TABLE(bsro) CLANG             /* Get the variable */
VGET (RCPUSER) TABLE(bsro) CLANG             /* Get the variable */
VGET (RPTBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (RPWUSER) TABLE(bsro) CLANG             /* Get the variable */
VGET (SHELLSRC) TABLE(bsro) CLANG             /* Get the variable */
VGET (TDBACKUP) TABLE(bsro) CLANG             /* Get the variable */
VGET (TDWHOME) TABLE(bsro) CLANG             /* Get the variable */
VGET (TWSBMPRIMSEL) TABLE(bsro) CLANG             /* Get the variable */
VGET (TWSTDWBIN) TABLE(bsro) CLANG             /* Get the variable */
VGET (WORK) TABLE(bsro) CLANG             /* Get the variable */
VGET (WTPARMS) TABLE(bsro) CLANG             /* Get the variable */
VGET (WTSUBMIT) TABLE(bsro) CLANG             /* Get the variable */
VGET (XFERDIR) TABLE(bsro) CLANG             /* Get the variable */
/**********************************************************************/

