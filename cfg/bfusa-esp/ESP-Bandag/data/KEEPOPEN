APPL KEEPOPEN

TAG 'BANDAG'

NOTIFY FAILURE ABEND ALERT(REMD)

JCLLIB 'BANDAG.MVS.JCLLIB'
COPYJCL 'SYS3.ESP.COPYLIB'
TEMPLIB 'SYS3.ESP.TEMPLIB'
DOCLIB 'SYS3.ESP.DOCLIB'

/* CCCHK JOB(SWESP9TS) RC(0:16) OK CONTINUE
/*CCCHK JOB(GLJ10-) RC(0:5) OK CONTINUE
CCCHK RC(1:4095) FAIL CONTINUE

IF !ESPAPSUB# > 1 THEN JCLLIB 'SYS3.ESP.COPYLIB'

OPTIONS RESTARTSTEP

JOB KEEPOPEN LINK
  RUN DAILY
  DELAYSUB 5:59 CENTRAL
ENDJOB

