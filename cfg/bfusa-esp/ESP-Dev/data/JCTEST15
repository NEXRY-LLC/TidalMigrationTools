APPL JCTEST15 WAIT NOPOST_WHILE_WAITING

INVOKE 'SWS.ESP.APPL.PROCLIB(#DISTRIB)'

/**********************************************************************/
/* THIS IS A TEST OF THE ESP SCHEDULING SYSTEM                        */
/**********************************************************************/
/*08/23/2016-TESTING ABANDON SUBMISSION STATEMENTS                    */
/**********************************************************************/

NOTIFY FAILURE ABEND MAILBOX(COSTELLOJOHN)
TAG TEST

JOB INTEFACE_EXTERNAL LINK
  RUN TODAY
  RELEASE ADD(SWESPJC2.202)
  ABANDON SUBMISSION 11AM
ENDJOB

JOB SWESPJC2.202
  CCCHK RC(5:4095) FAIL
  NOTIFY OVERDUE MAILBOX(C0STELLOJOHN)
  RUN TODAY
  RELDELAY 5
  RELEASE ADD(NEXT_JOB)
ENDJOB

JOB SWESPJC3
  PREREQ (SWESPJC1) COND(RC(00))
  CCCHK RC(5:4095) FAIL
  NOTIFY OVERDUE MAILBOX(C0STELLOJOHN)
  RUN TODAY
  RELEASE ADD(SWESPJC2)
  RELEASE ADD(NEXT_JOB)
ENDJOB

JOB NEXT_JOB LINK
  RUN TODAY
ENDJOB

