APPL JCTEST51 WAIT

INVOKE 'SWS.ESP.APPL.PROCLIB(#DISTRIB)'

/*****************************************************/
/* THIS IS A TEST OF THE ESP SCHEDULING SYSTEM       */
/*****************************************************/
TAG TEST

JOB LIS.!ESPAPPL LINK PROCESS
   RUN DAILY
   RELEASE ADD(NTJOB51)
ENDJOB

NT_JOB NTJOB51
  AGENT CYBA_PPIWPROD
  CMDNAME X:\cmd_scripts\LinkedInEnd.cmd
  USER maestro
  EXITCODE 0-1 success
  RUN today
/*IF EXITCODE 0 THEN NOTIFY END MAILBOX(COSTELLOJOHN)
/*IF !MNCMPC='0' THEN NOTIFY END MAILBOX(COSTELLOJOHN)
/*NOTIFY END MAILBOX(COSTELLOJOHN)
/*NOTIFY END MAILBOX(COSTELLOJOHN) COND(RC(1))
/*NOTIFY END ALERT(WA01)
  IF !MNRC='1' THEN NOTIFY END MAILBOX(COSTELLOJOHN)
  RELEASE ADD(LIE.!ESPAPPL)
ENDJOB

JOB LIE.!ESPAPPL LINK PROCESS
   RUN DAILY
ENDJOB

