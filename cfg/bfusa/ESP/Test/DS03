APPL DS03 JOB_ANCESTOR_WAIT

/* ALERT FOR 1ST JOB IN DSDJDA01 WHEN IT RUNS LONGER THAN 10 MINUTES
/* ALERT FOR APPL DSDJDA01 WHEN IT RUNS LONGER THAN 30 MINUTES
/* WILL GENERATE CONSOLE MESSSGAE TO CONTACT D. WILLARD IMMEDIATELY.

JCLLIB 'FTRF.PRDJOBS'
COPYJCL 'SWS.ESP.COPYLIB'
TEMPLIB 'SWS.ESP.TEMPLIB'
DOCLIB 'SWS.ESP.DOCLIB'

IF !ESPAPSUB# > 1 THEN JCLLIB 'SWS.ESP.COPYLIB'

OPTIONS RESTARTSTEP

IF !MNAPPL = 'JDAOQ002' THEN -
  DO
    ESP AJ ALL COMPLETE APPL(JDAOQ002.0)
  ENDDO

IF !MNAPPL = 'JDAOQ001' THEN -
  DO
    ESP AJ ALL COMPLETE APPL(JDAOQ001.0)
  ENDDO

IF (!MNAPPL EQ 'DSDNCDLD') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(DSDNCDLD.0)
  ENDDO

IF (!MNAPPL EQ 'DSDNCDLE') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(DSDNCDLE.0)
    EXIT
  ENDDO

IF (!MNAPPL EQ 'DSDNCDLF') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(DSDNCDLF.0)
  ENDDO

IF (!MNAPPL EQ 'DSDNCDLG') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(DSDNCDLG.0)
  ENDDO

IF (!MNAPPL EQ 'DSDNCDLH') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(DSDNCDLH.0)
  ENDDO

IF (!MNAPPL EQ 'DSDNCDLI') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(DSDNCDLI.0)
  ENDDO

IF (!MNAPPL EQ 'JDAOQ001') THEN -
  DO
    ESP AJ ALL COMPLETE APPL(JDAOQ001.0)
  ENDDO

