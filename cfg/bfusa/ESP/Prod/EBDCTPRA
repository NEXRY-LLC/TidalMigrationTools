APPL EBDCTPRA WAIT

NOTIFY FAILURE ABEND ALERT(REMP)

JCLLIB 'FTRF.PRDJOBS'
COPYJCL 'SWS.ESP.COPYLIB'
TEMPLIB 'SWS.ESP.TEMPLIB'
DOCLIB 'SWS.ESP.DOCLIB'

IF !ESPAPSUB# > 1 THEN JCLLIB 'SWS.ESP.COPYLIB'

OPTIONS RESTARTSTEP

JOB EBDCTPRA
    /*@@ SYSTEM=DC TIRE PLUS RETAIL INVOICE
    CCCHK RC(2:4095) FAIL CONTINUE
    RUN TODAY
    RELEASE ADD(EBDCTPR)
ENDJOB

                                                                        00713534
JOB EBDCTPR
    CCCHK RC(2:4095) FAIL CONTINUE
    RUN TODAY
ENDJOB

