APPL SWESPR23 JOB_ANCESTOR_WAIT

/*NOTIFY FAILURE ABEND ALERT(REMD)


JCLLIB 'FTRF.PRDJOBS'
COPYJCL 'SWS.ESP.COPYLIB'
TEMPLIB 'SWS.ESP.TEMPLIB'
DOCLIB 'SWS.ESP.DOCLIB'

IF !ESPAPSUB# > 1 THEN JCLLIB 'SWS.ESP.COPYLIB'

OPTIONS RESTARTSTEP

JOB SWESPR23
  RUN NOW
  RELEASE ADD(SWESPR24)
ENDJOB

JOB SWESPR24
  RUN NOW
ENDJOB
